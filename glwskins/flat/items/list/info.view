widget(container_x, {
  hidden: !$self.enabled;
  style: "ListItem";

  widget(icon, {
    source: $self.metadata.icon;
    style: "ListItemIcon";
  });

  widget(label, {
    filterConstraintX: true;
    caption: $self.description;
    maxlines: 10;
  });
});
