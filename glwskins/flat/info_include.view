


#define InfoLabel() {
  shadow: true;
  outline: true;
  color: 0.6;
  size: 0.75em;
}


#define InfoLine(TITLE, VAL, HIDDEN=false) {
  widget(container_x, {
    tableMode: true;
    hidden: HIDDEN;
    spacing: 0.15em;
    widget(label, {
      InfoLabel();
      align: right;
      caption: TITLE + ":";
    });
    widget(label, {
      caption: VAL;
	  size: 0.75em;
    });
  });
}

#define InfoBar(TITLE, VAL) {
  widget(container_x, {
    spacing: 0.15em;
    tableMode: true;
    widget(label, {
      InfoLabel();
      align: right;
      caption: TITLE + ":";
	  size: 0.75em;
    });

    widget(container_z, {
      width: 6em;
      widget(quad, {
        color: 0.4;
      });
      widget(bar, {
        color1: 1;
        color2: 1;
	fill: VAL;
      });
    });
  });
}
